# Frontend (build-time). NUNCA colocar segredos aqui — vai para o bundle do cliente.
PUBLIC_SITE_URL="https://www.maloteeletronico.com.br"
# Endpoint da API de leads (mesma origem em producao via nginx /api)
PUBLIC_LEAD_ENDPOINT="/api/lead"
# Opcional: chave publica do Cloudflare Turnstile (pode ir ao cliente)
PUBLIC_TURNSTILE_SITEKEY=""
